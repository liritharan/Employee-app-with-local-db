name: task
description: A new Flutter application.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^0.4.4
  path_provider: ^1.6.5
  sqflite: ^1.3.0
  path: ^1.6.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^0.4.5
  build_runner: ^1.8.1

flutter:
  uses-material-design: true
